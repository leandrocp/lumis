defmodule {module_name}.MixProject do
  use Mix.Project

  # Generated by `mise run wasm-hex-stage` — do not edit.
  #
  # An application with no code: `priv/parsers` holds the manifest and the
  # parser it describes, which is where Lumis reads them from. Nothing starts,
  # nothing supervises, and a release carries it like any dependency's assets.
  def project do
    [
      app: :{app_name},
      version: "{hex_version}",
      elixir: "~> 1.15",
      description: "Lumis parser for {languages_text}",
      package: package()
    ]
  end

  def application, do: []

  defp package do
    [
      maintainers: ["Leandro Pereira"],
      licenses: ["MIT"],
      links: %{
        "Lumis" => "https://lumis.sh",
        "GitHub" => "https://github.com/leandrocp/lumis",
        "Upstream grammar" => "{git_url}"
      },
      files: ~w[mix.exs priv README.md LICENSE]
    ]
  end
end
