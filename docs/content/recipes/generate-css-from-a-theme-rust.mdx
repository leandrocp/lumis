---
title: Generate CSS from a theme (Rust)
description: Generate linked CSS from a built-in Lumis theme in Rust.
---



```rust
use lumis::themes;

let css =
    themes::get("catppuccin_latte").unwrap().css(true);
```
