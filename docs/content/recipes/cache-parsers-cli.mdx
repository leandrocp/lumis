---
title: Cache parsers (CLI)
description: Find the cross-runtime guide to preloading Lumis languages.
---



Preloading is a cross-runtime concern, not a CLI-only one. See
[Languages](/languages) for the choice between loading on demand and preloading,
the non-blocking startup APIs, the CLI command, `LUMIS_DATA_DIR`, Elixir
configuration, and the runtimes that need no parser cache at all.
